# Visual Grasshopper Workflow

## 📐 3-Step GH Pipeline

```
┌─────────────────────────────────────────────────────────────────┐
│                    GRASSHOPPER WORKFLOW                         │
└─────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────┐
│  STEP 1: LOAD REVIT EXPORT                                       │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│   [Toggle: Reload?]                                             │
│        ↓                                                         │
│   ┌────────────────────────┐                                    │
│   │  Python Component      │  from gh_helper import             │
│   │  load_input_data()     │  GrassholperDataHelper             │
│   │                        │                                    │
│   │  • Check data/ dir     │  helper.load_input_data()         │
│   │  • Read latest JSON    │                                    │
│   │  • Parse geometry      │                                    │
│   └────────────┬───────────┘                                    │
│                ↓                                                 │
│   ┌────────────────────────┐                                    │
│   │ OUTPUT: objects        │  List of walls/floors/openings    │
│   │ OUTPUT: count          │  Number of objects                │
│   │ OUTPUT: summary        │  JSON with metadata               │
│   └────────────────────────┘                                    │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────┐
│  STEP 2: MODIFY GEOMETRY IN GRASSHOPPER                          │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│   objects (from Step 1)                                         │
│        ↓                                                         │
│   [Slider: Scale 0.5-2.0] ──┐                                  │
│   [Toggle: Filter Walls?]   ├──→ ┌─────────────────┐           │
│   [Text: Custom ID]         │    │ Python Process  │           │
│                             │    │                 │           │
│                             └──→ │ • Scale coords  │           │
│   ┌─────────────────────────────┤ • Add props      │           │
│   │ Rhino.Geometry Display      │ • Update version│           │
│   │ (Optional visualization)    │ • Filter types  │           │
│   │                             │                 │           │
│   │ • Points from coords        └────────┬────────┘           │
│   │ • Lines/Polylines                   ↓                     │
│   │ • Color by type            processed_objects               │
│   │                                                             │
│   └─────────────────────────────────────────────────────────┘  │
│                                                                  │
│   DATA TABLE (optional)                                         │
│   ┌──────────────────────────────────────────────────────────┐ │
│   │ id      │ type  │ version │ properties                  │ │
│   ├─────────┼───────┼─────────┼──────────────────────────────┤ │
│   │ wall_1  │ Wall  │ 2       │ {length: 50.5, ...}         │ │
│   │ floor_1 │ Floor │ 1       │ {level: "Level 1", ...}     │ │
│   │ door_1  │ Door  │ 2       │ {family: "Single-Flush"}    │ │
│   └──────────────────────────────────────────────────────────┘ │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────┐
│  STEP 3: SAVE & EXPORT                                           │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│   processed_objects (from Step 2)                               │
│        ↓                                                         │
│   [Toggle: Export to AGOL?]                                     │
│   [Text: Service Title]     ──┐                                │
│                               ↓                                 │
│        ┌──────────────────────────────────┐                    │
│        │ Python Component: Save           │                    │
│        │                                  │                    │
│        │ helper.save_output_data()        │                    │
│        │ exporter.export_to_agol()        │                    │
│        │                                  │                    │
│        └──────────┬───────────────────────┘                    │
│                   ↓                                             │
│        ┌──────────────────────────────────┐                    │
│        │ OUTPUT: filepath                 │                    │
│        │ OUTPUT: agol_service_id          │                    │
│        │ OUTPUT: success (true/false)     │                    │
│        │ OUTPUT: message                  │                    │
│        └──────────────────────────────────┘                    │
│                   ↓                                             │
│        ✅ data/gh_outputs/gh_output_*.json                    │
│        ✅ ArcGIS Online Feature Service created              │
│        ✅ Public URL ready for viewing                       │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

---

## 🎛️ Full Grasshopper Definition Layout

```
┌────────────────────────────────────────────────────────────────────┐
│                    GRASSHOPPER CANVAS                              │
└────────────────────────────────────────────────────────────────────┘

INPUTS (Left Side)
├─ [Toggle] Reload Input
├─ [Slider] Scale Factor (0.5 - 2.0)
├─ [Toggle] Filter by Type
├─ [Text] Element Type Filter (e.g., "Wall")
├─ [Toggle] Add Timestamp
├─ [Toggle] Export to AGOL
└─ [Text] AGOL Service Title

                                        │
                                        ↓

PROCESSING (Center)
┌─────────────────────────────┐
│  Python Component 1         │
│  "Load Input"               │
│                             │
│  load_input_data()          │
│                             │
│  Outputs:                   │
│  • raw_objects              │
│  • object_count             │
│  • summary                  │
└────────┬────────────────────┘
         │
         ↓
┌─────────────────────────────┐
│  Python Component 2         │
│  "Process Geometry"         │
│                             │
│  modify_geometry()          │
│  • Scale coordinates        │
│  • Add properties           │
│  • Update versions          │
│                             │
│  Outputs:                   │
│  • processed_objects        │
│  • modified_count           │
└────────┬────────────────────┘
         │
         ↓
┌─────────────────────────────┐      ┌──────────────────┐
│  Rhino Geometry Component   │      │  Data Table      │
│  (Visualization)            │      │  (Properties)    │
│                             │      │                  │
│  • Point Display            │      │  id | type | v   │
│  • Curve Visualization      │      │  ───┼──────┼───  │
│  • Color by Element Type    │      │ w_1 │ Wall │ 2   │
│                             │      │ f_1 │Floor│ 1   │
│  Outputs:                   │      └──────────────────┘
│  • geometry_preview         │
│  • visual_feedback          │
└──────────────────────────────┘
                                    
         ↓
┌─────────────────────────────┐
│  Python Component 3         │
│  "Save & Export"            │
│                             │
│  save_output_data()         │
│  export_to_agol()           │
│                             │
│  Outputs:                   │
│  • output_file              │
│  • agol_service_id          │
│  • success (bool)           │
│  • message                  │
└─────────────────────────────┘

OUTPUTS (Right Side)
├─ [Display] geometry_preview
├─ [Display] properties_table
├─ [Display] output_file
├─ [Display] agol_service_id
├─ [Display] success_status
└─ [Display] error_messages
```

---

## 🔌 Component Connection Guide

### **Component 1: Load Input**
```
Inputs:
  [None - loads automatically]

Outputs:
  objects → Connect to Process Component
  count → Display or Data Table
  summary → Monitor panel
```

### **Component 2: Process/Modify**
```
Inputs:
  objects ← From Load Component
  scale_slider ← From GH Slider
  filter_toggle ← From GH Toggle
  custom_props ← From GH Text

Outputs:
  processed → Connect to Save Component
  preview → Connect to Rhino geometry display
  report → Display as text
```

### **Component 3: Save & Export**
```
Inputs:
  objects ← From Process Component
  agol_creds ← From Text panels
  service_title ← From GH Text

Outputs:
  filepath → File path text display
  agol_id → AGOL service ID
  success → Boolean indicator
  message → Status message
```

---

## 📊 Data Flow Diagram

```
REVIT EXPORT (Revit Model → JSON)
      │
      └─→ data/gh_inputs/gh_input_*.json
             │
             ↓
      ┌─────────────────────┐
      │  GRASSHOPPER LOAD   │ ← You are here
      │  (Python Component) │
      └──────────┬──────────┘
                 │
       (Raw geometry objects)
                 │
                 ↓
      ┌─────────────────────┐
      │  GRASSHOPPER MODIFY │
      │  (Python Component) │  Scale, filter, add properties
      │  (Rhino Display)    │  Visualize in 3D
      └──────────┬──────────┘
                 │
       (Modified geometry)
                 │
                 ↓
      ┌─────────────────────┐
      │  SAVE & EXPORT      │  Save to JSON
      │  (Python Component) │  Upload to AGOL
      └──────────┬──────────┘
                 │
         ┌───────┴───────┐
         ↓               ↓
    JSON File       AGOL Feature
    (Local)         Service
                    (Online)
```

---

## 🎨 Recommended Component Layout

```
┌──────────────────────────────────────────────────────────┐
│                   GRASSHOPPER CANVAS                     │
│                                                          │
│  INPUTS          PROCESSING           OUTPUTS           │
│  ────────        ──────────           ───────           │
│                                                          │
│  ┌──────────┐   ┌─────────┐                            │
│  │ Reload   │   │ Load    │                            │
│  │ Toggle   │──→│ Python  │─┐                          │
│  └──────────┘   │ Comp 1  │ │                          │
│                 └─────────┘ │                          │
│  ┌──────────┐               │   ┌──────────┐           │
│  │ Scale    │               └──→│ Process  │───┐       │
│  │ Slider   │┐      ┌─────────→│ Python   │   │       │
│  └──────────┘│      │          │ Comp 2   │   │       │
│              │      │          └──────────┘   │       │
│  ┌──────────┐│      │            │ ↓          │       │
│  │ Filter   │┼──────┘        ┌───────┐       │       │
│  │ Toggle   │              │ Rhino  │       │       │
│  └──────────┘              │ Display│       │       │
│                            │ Curves │       │       │
│  ┌──────────┐              └───────┘       │       │
│  │ AGOL     │                              │       │
│  │ Settings │───────────┐                  │       │
│  └──────────┘           │   ┌──────────┐   │       │
│                         └──→│ Save &   │───┼──────→│
│  ┌──────────┐              │ Export   │   │       │
│  │ Service  │──────────────→│ Python   │   │       │
│  │ Title    │              │ Comp 3   │   │       │
│  └──────────┘              └──────────┘   │       │
│                                          │       │
│                                          ↓       │
│                                    ┌──────────┐ │
│                                    │ Outputs: │←┘
│                                    │ • File   │
│                                    │ • AGOL   │
│                                    │ • Status │
│                                    └──────────┘
│                                                          │
└──────────────────────────────────────────────────────────┘
```

---

## 🏃 Quick Setup Checklist

- [ ] Grasshopper is installed & opened
- [ ] Python component is available (GH 2.0+ recommended)
- [ ] Path to scripts is correct in `sys.path.append()`
- [ ] `data/` directory exists with revit_to_gis files
- [ ] Revit export (gh_input_*.json) exists in data/
- [ ] Create Python component (1)
- [ ] Create Python component (2)  
- [ ] Create Python component (3)
- [ ] Connect components together
- [ ] Test with "Reload" toggle
- [ ] Verify output appears in console
- [ ] Check data/gh_outputs/ for saved file
- [ ] Celebrate! 🎉

---

**Next: Copy a template from GRASSHOPPER_GUIDE.md into GH Python component**
